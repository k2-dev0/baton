{
  "name": "codex-baton",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "description": "Baton is a fail-closed model-switching proxy for Codex Desktop and CLI app-server clients.",
  "bin": {
    "baton": "bin/baton"
  },
  "engines": {
    "node": ">=20"
  },
  "scripts": {
    "check": "node --check src/policy.mjs && node --check src/proxy.mjs && node --check src/websocket.mjs",
    "test": "vitest run tests/proxy.test.mjs"
  },
  "devDependencies": {
    "vitest": "^4.1.11"
  }
}
